# Format: <ending-crumbs> = <new-tag-name>
# the new tag name replaces the last tag in the crumbs

# File must in UTF-8
# Empty lines and lines starting with # are ignored
# Casing is sensitive
# Spaces and '=' are not allowed in the tag names (new or old)
# List longest crumbs first

/figgroup/title = figtitle
